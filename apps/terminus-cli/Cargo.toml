[package]
name = "terminus-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow.workspace = true
clap.workspace = true
tokio.workspace = true
terminus-core = { path = "../../crates/terminus-core" }
