{
  "$schema": "https://schemas.tauri.app/config/2.x/capability.json",
  "identifier": "default",
  "description": "Default Terminus capability set",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:allow-open"
  ]
}
