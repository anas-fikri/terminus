{
  "name": "terminus-tauri",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "node scripts/tauri-proxy.mjs",
    "tauri:dev": "node scripts/tauri-dev-safe.mjs",
    "tauri:dev:safe": "node scripts/tauri-dev-safe.mjs",
    "build:mac": "npm run tauri -- build --bundles app,dmg",
    "build:mac:installer": "npm run tauri -- build --bundles app && node scripts/make-macos-pkg.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2.7.1",
    "@tauri-apps/plugin-shell": "^2",
    "@types/dompurify": "^3.0.5",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-web-links": "^0.12.0",
    "@xterm/xterm": "^6.0.0",
    "dompurify": "^3.4.2",
    "marked": "^18.0.3",
    "mermaid": "^11.14.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "^5.4",
    "vite": "^5.2"
  }
}
